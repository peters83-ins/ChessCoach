# Optional future coaching configuration. Local chess needs none of these.
OPENAI_API_KEY=
OPENAI_MODEL=
# Absolute executable path supplied locally; no engine is downloaded by the app.
STOCKFISH_PATH=
